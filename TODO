- stdlib.c:
  * implement arc4random()
  * use it to initialize SSP on remaining platforms
- string.c:
  * refactor code? (strchr/strrchr/memchr)

Platform specific:
- Linux:
  * fork() and pause() may have to be inline (asm/unistd.h)
